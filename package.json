{
  "name": "mirachan-homepage",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply mira-schedule --local",
    "db:migrate:remote": "wrangler d1 migrations apply mira-schedule --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
